use crate::gui::state::Tab;
use crate::gui::theme::{ModernTheme, StyleConstants};
use egui::{Color32, Rounding, Stroke, Vec2, Pos2, Shape, Align2, FontId};

// This module is now simplified and only contains tab-related utilities
// The main tab rendering is now handled directly in the main GUI module